# Face-Authorization-App
A modern, fast, and secure facial recognition web application built using Vanilla JavaScript, HTML5, and CSS3. This project uses `face-api.js` to detect and match facial features entirely in the browser without relying on any external backend databases.
